# คัดลอกเป็น .env — ห้าม commit .env จริง
# URL backend ไม่มี trailing slash; ไม่ต้องใส่ /api ต่อท้าย (โค้ดต่อให้แล้ว)
VITE_API_URL=http://localhost:3000

# Socket (ถ้าเปิดใช้)
# VITE_SOCKET_ENABLED=false
# VITE_SOCKET_URL=http://localhost:3000
